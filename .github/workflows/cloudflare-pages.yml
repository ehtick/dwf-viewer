name: Build Cloudflare Pages demo

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build-demo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run build:demo
      - uses: actions/upload-artifact@v4
        with:
          name: demo-dist
          path: demo-dist
